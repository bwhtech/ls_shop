# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.py~

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
tags
MANIFEST

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Dependency directories
node_modules/
# bench symlinks public/node_modules to the app root; the trailing-slash rule above
# only matches real directories, so the symlink needs its own line.
node_modules
jspm_packages/

# IDEs and editors
.vscode/
.vs/
.idea/
.kdev4/
*.kdev4
*.DS_Store
*.swp
*.comp.js
.wnf-lang-status
*debug.log

# Helix Editor
.helix/

# Aider AI Chat
.aider*

ls_shop/public/css/tailwind.css

# Local agent config
.claude/

# Vue dashboard build output — generated by `yarn build` in dashboard/, never committed.
ls_shop/public/dashboard/
ls_shop/www/dashboard.html

# The Pixio vendor theme, kept locally as the provenance reference the theme templates cite.
# 14MB of demo pages and duplicated CSS that nothing loads at build or run time.
reference_themes/
