{
	"name": "ls_shop-themes",
	"version": "1.0.0",
	"private": true,
	"description": "Tailwind toolchain for ls_shop themes. Build each theme from inside its own directory - running from ls_shop/themes/ moves Tailwind v4's source-detection root and every theme absorbs the others' utilities.",
	"scripts": {
		"build:shop_default_theme": "cd shop_default_theme && npx @tailwindcss/cli -i tailwind.input.css -o ../../public/themes/shop_default_theme/tailwind.output.css --minify"
	},
	"devDependencies": {
		"@tailwindcss/cli": "^4.0.14",
		"tailwindcss": "^4.0.14"
	}
}
